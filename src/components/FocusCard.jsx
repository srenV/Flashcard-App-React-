/*
  FocusCard (placeholder)
  - Small component planned to encapsulate card-focused UI. Currently a
    placeholder to avoid empty module errors and to signal intended behavior.
*/
import React from "react";

export const FocusCard = ({ card, onToggle }) => {
  // This is intentionally minimal. Replace with a full implementation if
  // you want to extract the focused-card UI from `LeftSection`.
  return null;
};
